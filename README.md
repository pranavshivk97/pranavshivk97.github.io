# Portfolio site (deployed)

This branch is published via GitHub Pages. It holds the built static
portfolio site — `index.html` plus the `CNAME` for the custom domain.

Source branch `master` keeps the previous React portfolio code.
