import { Box } from "@chakra-ui/react";
import ProjectGrid from "./ProjectGrid";

const Projects = () => {
  return (
    <Box m={10}>
      <ProjectGrid />
    </Box>
  );
};

export default Projects;
